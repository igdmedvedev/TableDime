services:
  app:
    build: .
    container_name: purchases-service
    ports:
      - "8080:8080"
    environment:
      - DB_HOST=purchases
      - DB_PORT=5432
    networks:
      - app-network

networks:
  app-network:
    external: true
    name: common-network